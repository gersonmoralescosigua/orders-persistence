spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orders
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  cloud:
    stream:
      default-binder: none  # Desactiva Solace en tests